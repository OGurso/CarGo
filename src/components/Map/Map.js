import React from "react";
import styled from "styled-components";

const StyledMap = styled.div`
  background-color: grey;
  width: 100vw;
  height: 100vh;
`;

const ViewMap = () => {
  return <StyledMap>Map goes here.</StyledMap>;
};

export default ViewMap;
